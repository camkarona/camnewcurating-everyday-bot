# 캄보디아 뉴스 브리핑 봇 - 프로젝트 설계 문서
> 작성일: 2026.05.29  
> 프로젝트명: 까로나 뉴스봇 MVP 1  
> 담당: 까로나 (정인휴) - 차세대리더네트워크 AI분과위원장

---

## 1. 프로젝트 배경 및 목적

### 배경
- AI 분과위원장으로서 실용적인 AI Agent 구현 필요
- 기존 보유 AI 활용: 텔레그램 /go 명령어 기반 Gemini API 봇 운영 중
- 뉴스 편집장이 매일 여러 뉴스 사이트를 돌아다니며 큐레이션하는 데 1~2시간 소요

### 목적
- 편집장에게 매일 아침 캄보디아 관련 뉴스를 자동으로 브리핑
- "AI가 밥상 차려주고, 사람이 먹을 것 고른다" 방식
- 자동 발행이 아닌 **편집장 보조** 도구 (사람의 판단 유지)

### 발전 방향
```
MVP 1: 뉴스 수집 → 요약 → 텔레그램 전송 (자동 브리핑)
MVP 2: 버튼 인터랙션 → 기사 초안 생성 → 채널 발행
MVP 3: 구독자용 유료 채널 자동화
```

---

## 2. AI Agent 개념 정리

### 일반 자동화 vs AI Agent 차이

| 구분 | 설명 | 까로나님 현재 앱 |
|------|------|------|
| AI 호출 | LLM API 단순 사용 | ✅ 텔레그램 /go 봇 |
| **AI Agent** | 목표 주면 → 스스로 계획 → 도구 사용 → 결과 반환 | ❌ 아직 없음 |

**핵심 차이: "도구를 스스로 골라서 쓰는가"**

---

## 3. MVP 1 전체 설계

### 실행 흐름

```
[매일 오전 11시 자동 실행]
        ↓
Google News RSS 수집
(키워드별로 최신 5개씩)
        ↓
중복 제거 + 24시간 이내 필터링
        ↓
Gemini API
→ 각 기사 한 줄 요약 (한국어)
→ 중요도 상/중/하 분류
        ↓
텔레그램 메시지 전송
(편집장 개인 채팅 or 비공개 그룹)
        ↓
[끝 - MVP 1은 여기까지]
```

### 키워드 구조

```python
KEYWORDS = {
    "캄보디아 뉴스 (한국어)": ["캄보디아", "프놈펜", "앙코르"],
    "Cambodia News (English)": ["cambodia", "phnom penh", "khmer"],
    "한-캄 관계": ["한국 캄보디아", "korea cambodia"]
}
```
> ※ 키워드는 언제든지 수정/추가 가능

### RSS 소스

```python
# 1. 캄보디아 영문 뉴스
"https://news.google.com/rss/search?q=cambodia&hl=en"

# 2. 한국 언론 캄보디아 언급
"https://news.google.com/rss/search?q=캄보디아&hl=ko&gl=KR"

# 3. 프놈펜 관련
"https://news.google.com/rss/search?q=프놈펜&hl=ko&gl=KR"
```

---

## 4. 텔레그램 출력 예시

```
📰 캄보디아 뉴스 브리핑 (05.29 / 11:00)
━━━━━━━━━━━━━━━

🇰🇭 캄보디아 현지 (영문)
⭐ [1] 프놈펜 신공항 2027 개항 최종 확정
   → 물류·부동산 섹터 직접 영향 예상
   🔗 링크

⭐ [2] 캄보디아 Q1 GDP 6.1% 성장
   → 제조업 회복세 주도
   🔗 링크

🇰🇷 한국 언론 속 캄보디아
▪️ [3] 한-캄 FTA 2라운드 협상 재개
   → 농산물·섬유 분야 쟁점
   🔗 링크

▪️ [4] 현대건설 프놈펜 대형 수주
   → 교민 취업 기회 증가 전망
   🔗 링크

━━━━━━━━━━━━━━━
총 8건 수집 / 중요 4건 선별
🤖 Gemini 요약 | 까로나 뉴스봇 v1
```

---

## 5. 기술 스택

| 항목 | 선택 | 이유 |
|------|------|------|
| 언어 | Python | 기존 봇 경험 있음 |
| RSS 수집 | `feedparser` | 설치 한 줄, 안정적 |
| AI 요약 | Gemini API | 이미 사용 중 |
| 텔레그램 | `python-telegram-bot` | 기존 경험 |
| 스케줄러 | Linux cron | Lightsail 기본 기능 활용 |
| 서버 | **AWS Lightsail** | 기존 보유, 추가 비용 없음 |

### 스크래핑 방식 선택 이유

| 방법 | 문제점 |
|------|------|
| 직접 웹 스크래핑 | CORS 차단, 봇 차단, 구조 변경, 법적 문제 |
| **Google News RSS** ✅ | 공식 제공, 차단 없음, 파싱 쉬움, 합법 |

---

## 6. 서버 설정 (AWS Lightsail)

### 파일 구조
```
/home/ubuntu/news_bot/
    ├── main.py              # 메인 실행 파일
    ├── config.py            # API 키, 키워드 설정
    ├── rss_collector.py     # RSS 수집
    ├── gemini_summary.py    # AI 요약
    └── telegram_sender.py   # 텔레그램 전송
```

### cron 설정
```bash
# crontab -e 에 한 줄 추가
0 11 * * * /usr/bin/python3 /home/ubuntu/news_bot/main.py
```
→ 매일 오전 11시 자동 실행

### 필요한 pip 패키지
```bash
pip install feedparser google-generativeai python-telegram-bot
```

---

## 7. 필요한 정보 (개발 시작 전 확인)

- [x] 텔레그램 봇 토큰 (확보 완료)
- [x] Gemini API 키 (기존 것 사용)
- [x] 텔레그램 전송 대상 chat_id (확보 완료)
- [ ] AWS Lightsail Python 버전 확인 (`python3 --version`) ← 배포 시 확인

---

## 8. MVP 2 예고 (추후 개발)

```
MVP 1 브리핑 수신
        ↓
버튼 선택: [1번 기사화] [2번 기사화]
        ↓
Gemini → 기사 초안 자동 생성
        ↓
[텔레그램 채널에 발행] 버튼
        ↓
원클릭 발행
```

---

## 9. 유료화 가능성

```
[무료] 텔레그램 공개채널
  → 하루 1회, 주요 헤드라인 3개 (맛보기)

[유료] 텔레그램 비공개채널 (월 $10~20)
  → 하루 2회, 전체 분석 + 한국어 인사이트
  → 분야별 (부동산 / 투자 / 정책 / 노무)
  → 편집장 코멘트 포함 (차별화 핵심)
```

---

## 10. 개발 진행 현황 (Claude Code)

> 최종 업데이트: 2026.05.29

### 📊 진행 현황 한눈에 보기

| 단계 | 내용 | 상태 |
|------|------|------|
| 1. 설계 | 사양 확정 (현지 7 영문+코멘트 / 한국 3 / 2메시지) | ✅ 완료 |
| 2. 코드 작성 | `camnewcurating_everyday_bot.py` 올인원 | ✅ 완료 |
| 3. 로컬 환경 | Python 3.14.5 + 패키지 설치 | ✅ 완료 |
| 4. 로컬 테스트 | 수집→요약→텔레그램 전송 정상 | ✅ 완료 |
| 5. 깃허브 | 비공개 저장소 업로드 (.env 제외) | ✅ 완료 |
| 6. 서버 배포 | Lightsail에 clone + .env + 설치 + 실행 성공 | ✅ 완료 |
| 7. 자동화 | cron 매일 캄보디아 06시 전송 등록 (편집장 요청) | ✅ 완료 |
| 8. MVP 2 | 버튼 → 기사 초안 → 채널 발행 | 🔮 추후 |

### 🗺️ 시스템 구조 (어디서 무엇이 도는가)

```
 ┌─────────────────┐   git push    ┌──────────────────┐   git pull   ┌──────────────────────────┐
 │  내 PC (로컬)    │ ───────────▶  │   GitHub          │ ──────────▶ │  AWS Lightsail 서버       │
 │  - 코드 작성/수정 │               │  (코드 중간 창고)  │             │  (24시간 켜져 있는 컴퓨터) │
 │  - Python 테스트  │ ◀───────────  │  public 저장소     │             │  - cron이 매일 11시 실행   │
 └─────────────────┘               └──────────────────┘             │  - .env(키)는 여기에만    │
                                                                      └────────────┬─────────────┘
                                                  매일 캄보디아 11시 cron 작동 시 ↓
                                ┌──────────────────────────────────────────────────────────────┐
                                │  ① Google News RSS  →  뉴스 수집                                │
                                │  ② Gemini API       →  현지 영문 코멘트 + 중요도               │
                                │  ③ Telegram API     →  단톡방(나+편집장+봇)에 메시지 2개 발송  │
                                └──────────────────────────────────────────────────────────────┘
```

**핵심 개념 3가지**
- **로컬 PC = 작업실**: 코드를 쓰고 고치는 곳. 여기서 `git push`로 GitHub에 보냄.
- **GitHub = 중간 창고**: 코드를 보관/전달. 로컬과 서버를 잇는 다리. (키는 안 들어감)
- **Lightsail 서버 = 24시간 일하는 직원**: 내 PC가 꺼져 있어도 서버는 항상 켜져 있어, cron이 매일 알아서 봇을 실행. 실제 키(`.env`)는 이 서버에만 존재.

**왜 굳이 서버가 필요한가?** 내 PC는 끄면 멈추지만, 봇은 매일 11시에 자동 실행돼야 하므로 "항상 켜진 컴퓨터(서버)"가 필요하다. 그래서 클라우드 서버(Lightsail)에 올린 것.

### 🧰 개발/배포 도구 환경 (오늘 사용)

| 용도 | 오늘 사용한 것 | 더 편한 대안 |
|------|---------------|--------------|
| 서버 접속·명령 실행 | Lightsail **웹 터미널**(브라우저) | VS Code **Remote-SSH** (실제 편집기로 접속) |
| 서버 파일 편집 | `nano` (터미널 편집기) | VS Code 편집창 (마우스/복붙 자유로움) |
| 복붙 문제 | 웹 터미널의 붙여넣기 표식(`^[[200~`)으로 토큰 깨짐 | VS Code/SSH는 이 문제 없음 |

> ※ 노트북엔 VS Code Remote-SSH가 이미 설정돼 있으므로, 다음부터는 **노트북에서 접속하면 .env 편집·토큰 입력이 훨씬 수월**하다. (오늘 겪은 웹 터미널의 복붙 글리치를 피할 수 있음)

### ✅ 완료된 것

- **코드 작성 완료**: `camnewcurating_everyday_bot.py` (올인원 단일 파일)
- **로컬 PC 환경 구축**: Python 3.14.5 설치, 패키지 4종 설치 완료
- **로컬 실행 테스트**: RSS 수집 ✅ / Gemini 요약 ✅ / 텔레그램 전송 ✅ (chat_id 확보 후 정상)
- **깃허브 업로드 완료**: https://github.com/camkarona/camnewcurating-everyday-bot
  (`.gitignore`로 `.env` 등 민감정보 제외 확인 완료. 서버 배포 편의를 위해 public으로 전환 — 코드에 비밀정보 없음)
- **서버 배포 완료**: Lightsail(싱가포르, UTC)에 git clone → venv 생성 → 패키지 설치 → `.env` 작성 → 실행 성공 (단톡방 수신 확인)
- **자동화 완료**: cron 등록 `0 23 * * *` (서버 UTC 23시 = 캄보디아 익일 06시, 편집장 요청), 로그는 `bot.log`
  - (변경 이력: 최초 `0 4`=캄 11시 → 편집장 요청으로 `0 23`=캄 06시)
- **전송 대상**: 텔레그램 단톡방(나+편집장+봇), group chat_id 사용 → 그룹 멤버 전원 수신

### 📂 실제 파일 구성 (설계 6장과 달라진 점)

설계 문서는 5개 파일 분리였으나, **관리 편의를 위해 단일 파일(올인원)로 통합**함.

```
005 뉴스브리핑 뉴스대령봇/
    ├── camnewcurating_everyday_bot.py   # 메인 (수집+요약+전송 올인원)
    ├── .env                              # 실제 키 (깃에 올리지 말 것)
    ├── .env.example                      # 키 템플릿
    ├── requirements.txt                  # 의존 패키지
    ├── README.md                         # 설치/배포 가이드
    └── cambodia_news_bot_design.md       # 이 설계 문서
```

### 🔧 설계 대비 변경/확정 사항

| 항목 | 설계 문서 | 실제 구현 (확정) |
|------|-----------|------------------|
| 파일 구조 | 5개 파일 분리 | **단일 파일(올인원)** |
| AI SDK | `google-generativeai` | **`google-genai`** (신버전, gemini-3 지원) |
| 모델 | Gemini (미지정) | **gemini-3-flash-preview** |
| 키 관리 | config.py | **.env + python-dotenv** (키 노출 방지) |
| 텔레그램 | python-telegram-bot | **requests 직접 호출** (cron 안정성) |
| 메시지 구성 | 1개 메시지 2그룹 | **2개 메시지 분리 전송** (현지 먼저 → 한국 나중) |
| 현지(영문) | 한국어 번역 요약 | **영어 원문 제목 + 한국어 코멘트 한 줄 + 출처** |
| 한국 언론 | 한국어 요약 | **제목 + 출처만** (Gemini 미사용, 빠름/저렴) |
| 꼭지 수 | 소스별 5개 | **현지 7꼭지 / 한국 3꼭지** |
| 현지 선별 | 최신순 | 후보 12개 수집 → **Gemini 중요도순 상위 7** |
| 시간 필터 | 24시간 이내 | **최근 24시간 이내** (작성 시각 기준) 유지 |
| 표시 시각 | 미지정 | **캄보디아 시간(ICT, UTC+7)** |
| 실행 시각 | 오전 11시 | **캄 06시(`0 23`, 지난 24h) + 캄 15시(`0 8`, 지난 12h)** — v1.3 하루 2회 |
| 저장소 공개 | 미지정 | **public** (배포 편의, 비밀정보 없음) |
| 버전 표기 | v1 | **v1.3** (텔레그램 하단 표기) |
| 현지 영문 요약 | 제목 코멘트 1줄 | **검색 기반 한국어 2~3줄 요약** (v1.3, 인사이트 줄 제거) |

### 🔁 운영 메모 (앞으로)

- **모니터링**: 매일 캄보디아 06시 단톡방 확인. 문제 시 서버에서 `cat bot.log` 또는 `tail -n 30 bot.log`로 실행 로그 점검
- **코드 갱신 흐름**: 로컬에서 수정 → `git push` → 서버에서 `cd ~/camnewcurating-everyday-bot && git pull` (public이라 토큰 불필요)
- **수동 실행(테스트)**: 서버에서 `cd ~/camnewcurating-everyday-bot && venv/bin/python camnewcurating_everyday_bot.py`
- **설정값 변경(chat_id 등)**: 서버에서 `nano .env` 직접 수정 (`.env`는 깃에 없음)
- **서버 재부팅 필요 안내**가 떠 있었음(`System restart required`) → 한가할 때 한 번 재부팅 권장. cron은 재부팅해도 유지됨

### ✅ v1.2 배포 (콘텐츠 깊이 강화)

> 회의 확정(2026.05.29) → 브랜치 `v1.2-content-depth`에서 로컬 테스트 → **편집장 승인** → main 병합.
> 목적: **편집장 시간 절약** — 링크를 안 눌러도 내용 파악. 구조 변화 없음(one-shot 유지, cron 그대로).
> 적용: 서버 `git pull` 후 **다음 06시 발송부터 v1.2 적용**.

- **현지 영문 7꼭지 강화**: 원문 제목 + **한국어 핵심 2~3줄 요약** + **교민/투자 의미 한 줄** + 출처 + 링크
- **본문 확보 방식 = C안 (Gemini 검색 그라운딩)**: 우리가 직접 스크래핑하지 않고, Gemini의 Google 검색·읽기 기능으로 헤드라인+출처를 근거로 요약
  - 구현: `google-genai`의 `google_search` 그라운딩 도구 사용. 검색 품질을 위해 기사별 개별 호출 검토(현재는 배치 1회)
- **한국 언론 3꼭지**: 현행 유지 (제목 + 출처). *(한국어 기사는 이미 읽기 쉬움)*
- **실패/불확실 처리(폴백)**: 그라운딩이 기사를 못 찾으면 제목 기반 요약으로 자동 대체, 환각(추측) 금지 지시
- **알려진 리스크**: 아주 최신/소규모 기사는 검색이 약할 수 있음 → 헤드라인·출처·날짜로 앵커링 + 폴백으로 완화. 실제 품질은 서버 수동 실행으로 검증 후 조정
- **부수 효과**: Gemini 호출에 검색이 붙어 실행 시간·토큰 약간 증가 (일 1회·7건이라 영향 미미)

### ⏭️ 기타 To-Do

1. **(선택) 한국 언론 소스 정확도 개선** — 현재 한국어 검색에 베트남/연예 기사가 간혹 섞임. 신뢰 매체 필터 추가 가능
2. **(선택) 비공개로 되돌리기** — 원하면 SSH 배포키 설정 후 repo를 다시 private 전환 가능
3. **(추후) MVP 2** — 버튼 인터랙션 → 기사 초안 생성 → 채널 발행 (상주형 봇으로 구조 변경 필요)

### 📅 변경 이력 (Changelog)

**2026.05.29 — 집중 개발일 (0 → 운영까지)**
1. 로컬 PC 환경 구축: Python 3.14.5 + 패키지 4종 설치
2. 코드 작성(올인원) → 로컬 테스트 성공 (수집·요약·전송)
3. GitHub 저장소 생성 → 서버 배포 편의를 위해 **public** 전환 (비밀정보 없음)
4. AWS Lightsail 서버 배포: `git clone` → venv 생성 → 패키지 설치 → `.env` 작성 → 실행 성공
5. 텔레그램 **단톡방**(나+편집장+봇) 연결, group chat_id 사용
6. **v1.1**: 표시 시각 한국(UTC+9) → **캄보디아(UTC+7)**, 하단 버전 표기 추가
7. cron 자동화: 최초 캄 11시(`0 4`) → **편집장 요청으로 캄 06시(`0 23 * * *` UTC)**
8. **v1.2 (콘텐츠 깊이)**: 현지 영문에 **검색 기반 2~3줄 요약 + 💡교민 의미** 추가
   - 브랜치 `v1.2-content-depth`에서 개발·로컬 테스트 → **편집장 승인** → main 병합 → 서버 배포
9. 서버 v1.2 반영 완료, 2026.05.30부터 06시 자동 발송 시작

**2026.06.03 — v1.3 (인사이트 제거 + 하루 2회)**
- 💡 교민 의미(인사이트) 줄 **삭제** → 현지 영문은 **원제목 + 한국어 요약**만
- `--hours` 인자 추가로 시간 윈도우 가변화
- 발송 **하루 2회**: 캄 06시(지난 24h, `0 23`) + 캄 15시(지난 12h, `0 8`)
  - 서버 cron에 오후 줄(`0 8 ... --hours 12`) **추가**, 오전 줄은 기본 24h로 그대로
- 브랜치 `v1.3-twice-daily` → 편집장 승인 → main 병합

**2026.06.04 — v1.3.1 (안정성 패치)**
- 06시 발송에서 텔레그램 일시 연결 끊김(`RemoteDisconnected`)으로 영문 메시지 1건 누락 → `bot.log`로 원인 확인
- **전송 자동 재시도** 추가 (네트워크 예외/429/5xx → 최대 3회 백오프, 4xx는 즉시 실패). 출력 형식 변화 없음

> 📌 **버저닝 규칙(2026.06.04~)**: 자잘한 수정 → 패치(v1.3.1), 큰 수정 → 마이너(v1.4). 말 안 해도 자동 적용.

> 다음 관찰: v1.3 요약 품질, `(원문 확인 필요)`/폴백 빈도, 24h/12h 중복 정도, 한국 언론 노이즈, 재시도 로그.

---

*이 문서는 Claude.ai와의 대화를 기반으로 작성되었습니다.*  
*다음 단계: Claude Code에서 이 문서를 컨텍스트로 사용하여 코드 개발 진행*
